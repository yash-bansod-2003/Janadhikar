meta {
  name: update
  type: http
  seq: 4
}

put {
  url: http://localhost:5000/departments/{{DepartmentId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{AccessToken}}
}

body:json {
  {
    "description": "Coordinates rural infrastructure and sanitation projects."
  }
}
