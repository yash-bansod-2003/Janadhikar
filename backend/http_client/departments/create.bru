meta {
  name: create
  type: http
  seq: 1
}

post {
  url: http://localhost:5000/departments
  body: json
  auth: bearer
}

auth:bearer {
  token: {{AccessToken}}
}

body:json {
  {
    "name": "Panchayat Raj Department",
    "description": "Handles local development and village infrastructure projects."
  }
}
