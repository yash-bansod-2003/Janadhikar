meta {
  name: update
  type: http
  seq: 4
}

put {
  url: http://localhost:5000/projects/{{ProjectId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{AccessToken}}
}

body:json {
  {
    "progress_percentage": 35,
    "status": "ongoing"
  }
}
