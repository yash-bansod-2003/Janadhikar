meta {
  name: create
  type: http
  seq: 1
}

post {
  url: http://localhost:5000/facilities
  body: json
  auth: bearer
}

auth:bearer {
  token: {{AccessToken}}
}

body:json {
  {
    "name": "Primary Health Center",
    "description": "Community health center serving the village residents.",
    "type": "health",
    "is_active": true,
    "villageId": 1
  }
}
