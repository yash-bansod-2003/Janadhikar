meta {
  name: update
  type: http
  seq: 4
}

put {
  url: http://localhost:5000/facilities/1
  body: json
  auth: bearer
}

auth:bearer {
  token: {{AccessToken}}
}

body:json {
  {
    "description": "Expanded primary health center with maternal care services."
  }
}
